params {
  input = null
  outdir = 'results'
  min_genes = 200
  max_pct_mt = 20
  n_top_genes = 2000
  resolution = 0.5
}

profiles {
  docker {
    docker.enabled = true
    process.container = 'ad-singlecell:latest'
  }

  local {
    process.executor = 'local'
  }
}
